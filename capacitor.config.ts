import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raanaapp.ionic',
  appName: 'LinkedinCourse1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
